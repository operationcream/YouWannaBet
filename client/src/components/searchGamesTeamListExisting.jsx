import React from 'react';
// container to hold users live bets
const SearchGamesList = ({bets}) => (

  <div className="teamBetList">
    <h3>List of Bets</h3>
  </div>
);

export default SearchGamesList;
